import classes from "./TextArea.module.css"
import {ITextAreaProps} from "./TextArea.types"

export default function TextArea({}: ITextAreaProps) {
  return <div className={classes.container}></div>
}
